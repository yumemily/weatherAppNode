<div class='container d-flex flex-row justify-content-center'>
    <div class='flex-column text-center'>

        <h1 class='mt-5'>Weather at {{city}}</h1>

        {{#if error}}
        <h2>{{error}}</h2>
        {{/if}}

        {{#if data}}

        <div class='flex-column'></div>
        {{! NTS: I can not use #if [item] == '[something]'... need a helper}}
        {{!-- {{#if_eq data.icon 'clear-day'}}  NTS: I don't even need this I just need to inject data.icon into the url--}}
        <img src='https://darksky.net/images/weather-icons/{{data.icon}}.png'>
        {{!-- {{/if_eq}}  --}}

        <h2><b>{{temperature}}˚ {{summary}}.</b></h2>
        <h3>
            <b>Feels like</b> {{apparentTemperature}}˚
            <b>Low:</b> {{data.temperatureLow}}
            <b>High:</b> {{data.temperatureHigh}}
        </h3>
        <h3>{{data.summary}}</h3>
        {{/if}}

        <div class='d-flex flex-wrap mt-5 justify-content-center'>
            {{! Go through each object in the times array }}
            {{#each times}}
            <div class='flex-column m-4'>
                <h5>{{time}}</h5>
                <h5><b>{{temp}}˚</b></h5>
            </div>
            {{/each}}

        </div>
    </div>
</div>